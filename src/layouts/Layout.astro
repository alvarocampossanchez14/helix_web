<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/png" href="/logo.png" />
        <meta name="generator" content={Astro.generator} />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css" integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet" />
        <meta name="keywords" content="GTA V Roleplay, servidor GTA RP España, RP español +18, servidor GTA V con whitelist, rol GTA V adultos, comunidad roleplay GTA España, servidor RP serio, GTA 5 RP en español, whitelist GTA V RP, roleplay para mayores de edad, servidor RP +18 España, rol en GTA V online, comunidad GTA V RP">
        <meta name="description" content="Helix Studio es un servidor de GTA V Roleplay +18 con whitelist, optimizado para ofrecer la mejor experiencia de rol en español. Únete a nuestra comunidad y vive el mejor RP en GTA V.">
        <title>Helix Studio | +18 </title>
    </head>
    <body class="bg-black/50 text-white font-bold font-stretch-semi-expanded select-none scroll-smooth">
        <canvas id="plexus-background" class="absolute inset-0 w-full h-full z-0"></canvas>
        <div class="relative z-10">
            <slot />
        </div>
    </body>
</html>

<style>
    :root {
        --plexus-opacity: 0.4;
    }

    html,
    body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow-x: hidden;
    }

    body {
      background-color: #000;
      position: relative;
    }

    #plexus-background {
      position: fixed;
      top: 0;
      left: 0;
      z-index: -1;
      width: 100%;
      height: 100vh;
      opacity: var(--plexus-opacity);
    }

    body > div {
      padding: 1rem;
    }
</style>


<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const canvas = document.getElementById('plexus-background');
    const ctx = canvas.getContext('2d');
    let W, H, particles;

    function resizeCanvas() {
      W = window.innerWidth;
      H = window.innerHeight;
      canvas.width = W;
      canvas.height = H;
      if (!particles) init();
    }
    
    function init() {
      particles = [];
      for (let i = 0; i < (W * H) / 9000; i++) {
        particles.push(new Particle());
      }
    }

    function Particle() {
      this.x = Math.random() * W;
      this.y = Math.random() * H;
      this.vx = Math.random() * 1 - 0.5;
      this.vy = Math.random() * 1 - 0.5;
      this.radius = Math.random() * 2 + 1;
      this.color = `rgba(150, 200, 255, ${Math.random() * 0.7 + 0.3})`;
    }

    function draw() {
      ctx.clearRect(0, 0, W, H);
      for (let i = 0; i < particles.length; i++) {
        let p = particles[i];
        ctx.beginPath();
        ctx.fillStyle = p.color;
        ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2, false);
        ctx.fill();
        p.x += p.vx;
        p.y += p.vy;

        if (p.x < 0 || p.x > W) p.vx *= -1;
        if (p.y < 0 || p.y > H) p.vy *= -1;

        for (let j = i + 1; j < particles.length; j++) {
          let p2 = particles[j];
          let distance = Math.sqrt(Math.pow(p.x - p2.x, 2) + Math.pow(p.y - p2.y, 2));
          if (distance < 120) {
            ctx.beginPath();
            ctx.moveTo(p.x, p.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.strokeStyle = `rgba(255, 255, 255, ${1 - distance / 120})`;
            ctx.stroke();
          }
        }
      }
      requestAnimationFrame(draw);
    }
    
    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();
    init();
    requestAnimationFrame(draw);
  });
  
</script>